using UnityEngine;

public class PlayerSoundManager : MonoBehaviour
{
    private void Update()
    {
        if (ClientLoadedHandler.instance.allClientsLoaded)
        {
            
        }
    }
}
